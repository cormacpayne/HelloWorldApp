name: Trigger auto deployment for corm-aca-eastus2

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/corm-aca-eastus2-AutoDeployTrigger-71630508-4ec7-4aa9-b77c-c877bf69aab4.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CORMACAEASTUS2_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CORMACAEASTUS2_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CORMACAEASTUS2_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CORMACAEASTUS2_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CORMACAEASTUS2_REGISTRY_PASSWORD }}
          containerAppName: corm-aca-eastus2
          resourceGroup: corm-aca-bug-bash
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          


